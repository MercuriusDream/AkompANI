{
  "name": "canaria",
  "version": "0.1.0",
  "private": false,
  "description": "CANARIA — static-first visual agent IDE.",
  "scripts": {
    "dev": "vite --host --port 3000",
    "build": "vite build",
    "preview": "vite preview --host --port 4173"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "packageManager": "bun@1.3.3"
}
